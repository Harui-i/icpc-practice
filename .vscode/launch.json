{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug active file (CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/.build/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "stdio": [
        "${fileDirname}/sample.in",
        null,
        null
      ],
      "preLaunchTask": "debug build active file"
    }
  ]
}
